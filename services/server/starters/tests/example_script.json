{
    "name": "example_script",
    "parameters": [
        {
            "name": "script_path",
            "type": "text",
            "default": "",
            "validator": "return input && !/\\s/.test(input);",
            "description": "script_path should be nonempty and free of whitespace"
        }
    ],
    "validator": "console.log('Global validator'); return true;",
    "category": "script"
}
