{
    "name": "idle",
    "parameters": [
        {
            "name":"duration",
            "type":"text",
            "default": "",
            "validator": "return input && !/\\s/.test(input);",
            "description": "duration should be nonempty and free of whitespace"
        }
    ],
    "validator": "console.log('Global validator'); return true;"
}
